using System.Linq;
using SteamKit2;

namespace Core.Commands
{
    //class Help : CommandAction
    //{
    //    public override CommandResult Execute(Bot bot, SteamID caller, MessageParser parser)
    //    {
    //        // Returns a list of each command and it's arguments if specified.
    //        //return new CommandResult(Strings.Repo + " | " + Strings.Help + " " + string.Join(" // ", CommandRegistry.Commands.Select(x => x.Help).ToArray()));
    //        return new CommandResult("Help: " + string.Join(" // ", bot.GetModule<Command.Commands.Select(x => x.Help).ToArray()));
    //    }
    //}
}
